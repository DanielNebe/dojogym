import React from 'react';
import ReactDOM from 'react-dom';
import App from './App'; // Assuming you have an App component in the same directory

ReactDOM.render(<App />, document.getElementById('root'));
